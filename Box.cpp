//
// Created by Hillal Aimene on 2019-04-05.
//

#include "Box.h"

Box::Box(int i, int j, int value, bool visible, bool canChange) : i(i), j(j), value(value), visible(visible),canChange(canChange) {}